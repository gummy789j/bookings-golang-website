{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is about page.</h1>
                <p>hello</p>
                <p>test3</p>
                <p>This came from the template: {{index .StringMap "test" }}</p>
            <!--ne => not equal-->
                <p>
                    {{if ne (index .StringMap "remote_ip") ""}}
                        Your remote IP address is {{index .StringMap "remote_ip"}}
                    {{else}}
                        I don't know your ip address yet. Visit the <a href="/">Home page</a> so you can set it.
                    {{end}}
                </p>
            </div>
        </div>
    </div>
{{end}}